@model List<Reward>
@{
    ViewData["Title"] = "Review Rewards";
}

<nav>
    <ol class="breadcrumb">
        <li class="breadcrumb-item active">Review Rewards</li>
    </ol>
</nav>

@if (Model.Count == 0)
{
    <p>There are no rewards to review.</p>
}
else
{
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Name</th>
                <th>Submit Date</th>
                <th class="d-none d-md-table-cell">Creator</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var reward in Model)
            {
                <tr>
                    <td><a asp-action="Review" asp-route-id="@reward.Id">@reward.Name</a></td>
                    <td>@((reward.SubmitDate?? default(DateTime)).ToString("d"))</td>
                    <td class="d-none d-md-table-cell">@reward.Creator.Name</td>
                </tr>
            }
        </tbody>
    </table>
}
